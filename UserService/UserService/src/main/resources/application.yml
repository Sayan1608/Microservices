server:
  port: 8081

spring:
  application:
    name: USER_SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/service_user
    username: root
    password: sayan

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true


