server:
  port: 8082

spring:
  application:
    name: HOTEL_SERVICE
  datasource:
    url: jdbc:postgresql://localhost:5432/service_hotel
    username: postgres
    password: sayan

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
